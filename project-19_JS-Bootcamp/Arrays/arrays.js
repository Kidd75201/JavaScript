// **** Array challenge 1 ******
var numbers = [1, 2, 3, 4];
var revOrder = numbers[i] - 1;

function printReverse() {
  numbers.forEach(number => console.log(number));
}

for (let i = 0; i < numbers.length; i++) {
  //   console.log(numbers[i]);
  revOrder = numbers[i] - 1;
  console.log(revOrder);
}

// printReverse();

// **** Array challenge 2 ******

// **** Array challenge 3 ******

// **** Array challenge 4 ******
